# 怎么装（Mac）

## 一行，粘进终端就行

打开「终端」（启动台 → 其他 → 终端），粘这一行，回车：

```
bash ~/Downloads/装.command
```

**文件不在下载文件夹的话**：先打 `bash ` 再空一格，
然后把 `装.command` 那个文件**直接拖进终端窗口**，路径会自己出现，回车。

---

## 为什么不能直接双击

双击的话 Mac 会弹一句：

> 要查看或更改访问权限，请在"访达"中选择该文件，然后选取"文件">"显示简介"。

**这不是出错，是 Mac 在拦。**

脚本文件要能被双击运行，必须带一个叫「可执行」的标记。
仓库里那份是带着的，**但文件经过聊天窗口、邮件、网盘传给你的时候，
这个标记会被抹掉** —— 所有传输都这样，是常态，不是意外。

**而且 Mac 那句提示会把你带沟里**：「显示简介」那个面板只管
读/写权限，**改不了「能不能运行」这个标记**，照它说的做会白折腾一圈。

上面那一行为什么能绕开：
**双击 ＝ 让系统去「执行」这个文件**，要那个标记；
**`bash 文件名` ＝ 让 bash 去「读」这个文件然后照着做**，读不需要标记。
一个字的差别。

---

## 装完之后

**以后不用再碰这个文件了。** 桌面上会多一个 `MoneyLoop.app`，
那是安装程序**在你机器上现生成的** —— 生成的时候标记就打好了，
而且因为不是从网上下载的，**也不会弹「来自身份不明的开发者」那个框**。

以后天天双击桌面那个就行。

---

## 卡住了怎么办

安装程序每一步都会在屏幕上说人话。哪一步红了，**把那几行字截图发我**，
上面写着卡在哪。最后一步是跑一次自检 —— **自检没绿，它不会说"装好了"。**
