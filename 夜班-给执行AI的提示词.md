# 任务：把「夜班」做成真正无人值守的

**这一整段直接发给它就能开干。**

---

你接手的是一套叫 MoneyLoop 的系统。仓库分支：
`claude/project-template-automation-ea9utn`

**先读这四份，别读别的**：
`references/夜班.md`（这次的设计全文，最重要）·
`references/验证的规矩.md`（六条验收规矩，你必须照做）·
`工程交接包.md` 第五节和第八节 · `scripts/test-夜班.sh`（现有测试长什么样）

读完**先别动手**，按最后一节的规矩给我三样。

---

# 一句话目标

> **老板睡觉的时候，这套东西自己醒来、自己找活干、自己淘汰、
> 早上给他一页纸。整个过程一次都不问他。**

**"不问他"不等于"替他做主"。** 分界线在第一件活里写死了。

---

# 现在已经有什么（别重做）

```bash
./loop.sh 想法 "一句话"    # 收进想法箱，排队，不花钱
./loop.sh 夜班 8           # 轮转推进所有在跑的想法，最多 8 小时
./loop.sh 战报             # 一页纸：排行榜 ＋ 谁在等他
./loop.sh 砍 002 / 留 002  # 他早上唯一要做的动作
```

已经做好的机制：想法之间地盘隔离（`LOOP_HOME`）、工头轮转（跑得少的先跑）、
台账每轮落一行、五条停机条件（到点／轮数／预算／全停下／**自检红了不开工**）。
自测 18 项，`BREAK=1 bash scripts/test-夜班.sh` 能看它变红。

**这些不要改。你是在它上面接着盖。**

---

# 你要做的五件，按顺序，一次只做一件

## 第 1 件 · 沙盘戳（最重要，其余四件都靠它）

**要解决的问题**：现在一个想法撞到"需要老板拍板"的闸门就停下等他。
想法都停完了，夜班就结束了——**可能才跑了半小时。**
**"一直跑"和"等他点头"是打架的。**

**解法不是取消闸门，是让它带着一个记了名的假设继续往前推。**

### 规格

夜班模式下（给个环境变量，比如 `LOOP_SANDBOX=1`），撞到闸门时**不停**：

1. 让 AI **自己选一个最合理的假设**，追加进这个想法的 `docs/00-假设.md`，
   一条一行，字段固定：
   `编号 · 哪一步 · 假设内容 · 为什么选它 · 如果错了会怎样 · 时间 · 状态(未定/留/砍)`
2. 在这个想法的 `.loop/` 里记下 `假设_<步骤>=<编号>`，
   把该步标成"假设通过"（**不是"他确认了"——这两个状态必须能分开**）
3. **此后每一份产出的文件开头，必须盖一行戳**：
   `> ⚠ 这份东西建立在还没定的假设 #003 上，他一旦砍掉，这份跟着作废`
4. 早上他一句话定：`./loop.sh 定 003 留` / `./loop.sh 定 003 砍`
   - **留** → 戳撤掉，该步转成真的确认
   - **砍** → **连坐**：这条假设之后的所有阶段退回到那一步，
     产出**移到 `作废/` 目录，不许删**（他可能想翻）

### 验收（我会自己跑这几条）

```bash
# ① 数得出来现在有几条假设未定
./loop.sh 假设 | grep -c 未定

# ② 未定假设下的每一份产出都带戳（一份没带就算没做）
grep -L "⚠ 这份东西建立在还没定的假设" 赛马/*/docs/*.md

# ③ 砍掉一条假设，下游真的退回去了，而且产出还在
./loop.sh 定 003 砍 && ./loop.sh 战报 && ls 赛马/*/作废/
```

**测试硬要求**：`scripts/test-沙盘.sh`，并且 `BREAK=1` 能让它变红。
**注入点必须落在被测代码上，不许落在测试的期望值上。**
建议的注入点：**把盖戳那一步掐掉**——屏幕照样跑、假设照样记，
但产出不带戳，于是他看不出哪份东西是建在沙子上的。**这就是最贵的那种失败。**

### 这一件里最容易做错的地方

**不许把"假设通过"和"他确认了"存成同一个状态。**
存成一个，早上就分不清哪些是他真点过头的、哪些是系统自己蒙的——
**那等于闸门被悄悄删了，而且没人看得见。**

---

## 第 2 件 · 让它自己醒

现在还得人手动敲一次 `./loop.sh 夜班`。

做一条命令，把系统定时任务装上／看／卸掉：

```bash
./loop.sh 定时 装 23:00 8     # 每晚 11 点开跑，最多 8 小时
./loop.sh 定时 看
./loop.sh 定时 卸
```

**验收**：装完 `crontab -l` 里能看到那一行；卸完就没有了；
**重复装两次不许出现两行**（这个必须有测试）。

**边界**：只做定时启动。**不许在这一件里加任何"到点自动对外做事"的东西。**

---

## 第 3 件 · 战报升级成真正的一页纸

现在的战报只有排行榜。**目标是他早上花十五分钟就够。**

必须包含，缺一项都算没做完：

1. 昨晚跑了多少轮、花了多少、停在哪条停机条件上
2. 排行榜：每个想法一行（可行性 · 假设未定几条 · 分歧几处 · 一句话）
3. **最多三道选择题**。每道 2–3 个选项，每个选项写清楚
   **是什么（大白话）· 好在哪 · 代价是什么 · 你推荐哪个及为什么**
   —— **超过三道就是没替他想清楚，宁可少给。**
4. **它自己承认的**：最不确定的一条 ＋ 最可能白跑的一件事
5. 一句话：**「如果你今天只做一件事，做这个」**
6. **标准漂移警报**：连续 N 轮 `docs/03-什么算好.md` 的
   「标准修订记录」一条都没新增 → 报出来。
   **一整晚没改过一条标准，说明它要么还没碰到现实，要么在护着旧判断。**

**说话方式**：他不懂技术名词。**术语第一次出现必须当场翻译，最好带类比。**
每条判断标 `[事实]`／`[推断]`／`[猜测]`，猜测后面补一句"如果错了会怎样"。

---

## 第 4 件 · 把试金石接进夜班

`./loop.sh 试金石 "一段话"` 已经能让两个模型各拆一遍、自动数分歧。

接进夜班，**只在两个节点各对跑一次**：听懂那段话的时候、可行性判定之前。
**别每步都跑——那不是取分歧，是重复劳动。**

分歧数写进战报。**全票通过要报警，不许给绿灯**（这条现有代码里已经有了，别改坏）。

**这里的重点不是省钱**：老板的额度是订阅制、不按次收钱。
**接第二家模型买的是"错得不一样"**——一家模型自己跟自己商量永远很和睦。

---

## 第 5 件 · 红线自检

三条红线，任何情况不许自动跨过：
**花生意的钱 · 对外发东西 · 不可逆操作（删除、上线、签约）**

做一条 `./loop.sh 红线`，把**能查的都查了**：

- 夜班时段账本里有没有自动记进去的"支"
- 允许 AI 用的工具清单（`LOOP_ALLOWED_TOOLS`）里有没有能对外发东西的
- 有没有哪个提示词里写着"先做了再说""出问题再回滚"这类话

**查不了的，明明白白写"这条查不了，要靠人看"。**
**不许为了让报告好看，把查不了的写成"通过"。**

---

# 六条铁律（`references/验证的规矩.md`，你必须照做）

1. **一套测试必须证明它能变红**，不是只会绿。注入点落在被测代码上
2. **跳过不等于通过**。没跑成就是"没验"，总结里不许出现"全部通过"，退出码非 0
3. **文档里的数字，要么能被一条命令算出来，要么删掉**
4. **验收之前先写下预期答案，再跑**。没有预期答案的测试不是测试
5. **人说的话照样要核**。"我做了""已经好了"，去核，对不上就当面讲
6. **一个词只许有一个意思**。发现一词二义，先停下来把定义分开写清楚，
   **不许直接改规则**

---

# 三条不许

- **不许问老板问题。** 有疑问就选一个最合理的做法，
  **写进「我做的假设」清单跟着交回来**——跟第 1 件的沙盘戳是同一个道理。
  **唯二例外**：撞到红线、或者规格自相矛盾。这两种立刻停下来说。
- **不许一次做两件。**「顺便把文档也更新一下」是灾难的开始
- **不许在 `check.sh` 是红的时候往下做。** 先修红的

---

# 交付规矩

**开工前**，每件活先给我三样（顺序不能反）：

```
① 怎么做
② 我怎么验证它真的成了
③ 最可能出问题的地方
```

**先做完再想怎么验，你会给我一个刚好能通过的验法。**

**做完之后**，给我四样：

```
① 改了哪几个文件？各改了什么？
② 怎么验证的？把你实际跑的命令和【真实输出】贴出来（含退出码）
③ 有没有想做但没做成的？是什么？为什么？
④ 有没有可能弄坏别的地方？哪儿？
```

---

**现在别动手。** 先读那四份文件，然后就第 1 件（沙盘戳）给我三样。
