# 后续要发给它的每一条（按顺序，一条一条发）

**下面按顺序，一条一条发，别提前发下一条。**

> **已经走到哪了（2026-08-18）**：第 2、第 3 条已发；它复核完数字、
> 交了地基 1 的第一批测试（记账／止损／状态.py／数字漂移）、
> 修好了 check.sh「跳过被算成通过」的洞、还自己写了 `references/验证的规矩.md`。
> **下一条要发的是第 4 条。**

---

## 第 2 条 · 回应它的三个不确定 ＋ 派第一件活　【已发】

```
答得好，三点都对，而且你自己标了"数字是交接包报的、我还没复核"——
这正是第 6 条不许要的态度。

你逮到的那个编号重复（两个「第六节」）是真的，已经修了。
你没动手改，做对了。

你那三个不确定，全是我规格的缺口，不是你没读懂。
我在 工程交接包.md 新加了【第八节】专门回答，重新拉一下再读那一节。
先给你结论：

1. 钱：这是两种钱，分开管
   🟢 运行成本（AI 调用，含调第二家模型）→ 预算闸门管，闸门内全自动
   🔴 生意的钱（投放/采购/付服务费）→ 必须人批，永不下放
   分界线只有一条：这笔钱是为了让系统【想清楚】，
   还是为了【对外产生动作】？
   所以「风控换一家模型」不违反红线——那是运行成本。

2. 它跑在哪：先做 A（老板自己电脑 + 系统定时任务）。
   A 和 B（常开小机器）的代码是同一套，换机器不用重写。
   「待批」先用最省事的：写成文件同步到网盘/git，手机上打开就能看。
   别让通知渠道拖住核心功能。

3. 复核：你说得对，这是应该的。这就是你的第一件活。

现在派第一件活：把交接包里那些数字，全部自己数一遍。
第八节第 3 条有现成的命令。

按规矩五，先别动手，先给我三样：
怎么做 / 我怎么验证它真的成了 / 最可能出问题的地方。
```

---

## 第 3 条 · 它复核完之后（派地基第 1 件）　【已发】

```
好。现在做地基第 1 件：补测试。

范围：只补测试，不改任何现有功能代码。
目标：覆盖的命令数从 4 条涨到 ≥20 条。

一条硬要求：每套测试必须能测出「看起来对但其实没做」，
不是只测有没有报错。
参考现有的 scripts/test-接口.sh —— 它用一个假的 claude 二进制，
检查那个程序【真正收到的环境变量】是什么，
因为换接口最坏的失败方式不是报错，是"看起来换了其实没换"。

按规矩五，先给我三样。
```

---

## 第 4 条 · 对账 ＋ 把「覆盖率」变成一把尺子（下一条发这个）

```
对账，三条：一条我错，一条你没算到底，一条给你个定义。

1) 命令数 46 我认。你做对的不是数对，是【把定义写死了】——
   help 算不算一条命令，本来就不是数数问题，是定义问题。
   定义不写，这个数每换一个人数就变一次。
   （我这边按"所有 case 分支"数是 47，跟你的 46 差的正好是 help 那一条。
     同一份代码能数出两个数，说明定义没定之前，这个数字本身没有意义。
     以你写死的定义为准，不用改。）

2) 55 和 61 都对，因为量的不是同一样东西：
   55 是四套测试的子项，check.sh 自己还有大项，61 是屏幕上所有 ✓ 的行数。
   我原来写「61 项自测」，是把两把尺子的读数加在一起——这个是我错。
   顺带一件事：你写进第三节的「4 套 55 子项」，一天就烂了——
   你自己补完测试，现在是 8 套。我按你的规矩三，把这两个数从文档里删掉了，
   只留下有漂移测试盯着的 46。规矩是你写的，第一个被它逮到的是我们俩。

3) 你留的那个待核，我给个定义：auto|自动 不算定时器。
   它是个 while 循环，要人先敲一次才开始，撞到预算/拍板/卡住/做完就停，
   不会自己醒。已经写进第八节。

验证的规矩那六条我全收。尤其第 2 条——check.sh 在没装 pytest 时
报「全部通过」还退出 0，这个洞是你补的，记你头上。

一件事我这边改了，你别再手改：三个 交接包-*.txt 是【生成物】。
以后改内容改源文件，然后跑 bash scripts/打包交接.sh 重新生成。
手改生成物 = 同一份内容有两个真身，正是你规矩三骂的那件事。

现在把地基第 1 件收尾。目标不是"再多写几套测试"，
是把【覆盖率本身变成一把尺子】：

  · 给出一条命令，能算出「46 条命令里，有几条被测试真正跑过」
  · 照 test-数字漂移.sh 的样子把它钉进 check.sh：
    文档写的覆盖数和实数对不上就变红
  · 然后把覆盖数推到 ≥20

硬要求不变：每套新测试都要能证明自己会变红。BREAK=1 那个做法很好，保持。

按规矩五，先给我三样。
```

## 第 5 条 · 测试做完（地基第 2 件）

```
做地基第 2 件：CI。

每次推代码自动跑 bash scripts/check.sh。

验收标准就一条：推一个故意写坏的 commit，CI 必须变红。
你做完之后，自己推一个坏的验一次，把红的截图或输出贴给我。

按规矩五，先给我三样。
```

---

## 第 6 条 · 地基好了，上核心一（最重要的一条）

```
地基够用了，上核心一。

先做最小的一步，不要整个做完：
把「风控」这一个角色换成跟别人不同的模型，然后验证"独立"是不是真的。

验证方法（这条是硬的，不许改）：
拿同一个议题跑两次论证——
  一次：所有角色都用同一个模型
  一次：风控换成另一家
两次的【分歧数】必须明显不同。

不同 → 证明"独立"是真的，我们再往下铺
没差别 → 说明问题不在模型在提示词，那铺开也没用，回来告诉我

这一步就是核心一的试金石。先别做自动召集，先把这个验了。

按规矩五，先给我三样。
```

---

## 第 7 条 · 独立性验证通过（核心一剩下两层）

```
好，独立是真的。现在补核心一剩下的两层。

（1）自动召集：跑完一步之后，系统自己判断要不要开会。
    触发条件必须是可数的，不许靠感觉。至少这四条：
      · 承重条件里 ≥2 条还是 [猜测]
      · 可行性 < 90%
      · 风控提了反对但没人回应
      · 连续 3 轮「标准修订记录」没新增

（2）分歧率统计：每轮论证之后报出
      完全一致 N 条 / 有分歧 N 条 / 无人回应 N 条
    并且：全票通过时必须有明显警告——
    "全票通过"应该触发警报，不是让人放心。

这两件可以一起做，因为（2）是（1）的判断依据。

按规矩五，先给我三样。
```

---

## 第 8 条 · 上核心二（先只做定时器）

```
核心一够用了，上核心二。还是先做最小的一步。

只做第一件：让它能自己醒。

用老板自己电脑上的系统定时任务（crontab / 任务计划程序），
每天固定时间跑一次 ./loop.sh 日报。

验收：设好之后，人什么都不做，第二天日报自己出来了。

先别做「对外动作」和「待批队列」，先把"会自己醒"这件事跑通。

按规矩五，先给我三样。
```

---

## 第 9 条 · 定时器通了（待批队列）

```
好。现在做核心二的第二件：待批队列。

模式是「备好，等他点头」：
系统跑到最后一步，把动作【整个备好】——
内容写好、平台选好、时间定好——打包成一个「待批」。

关键在"备好"：不是问他"要不要发个内容"，
而是全都准备好了，他只需要看一眼说"发"。

待批先用最省事的送达方式：写成文件，同步到网盘或 git，
手机上打开就能看。别做推送，那是后面的事。

红线不许破：待批里任何一条【对外发布 / 花生意的钱 / 不可逆】的动作，
在没有人明确点头之前，绝对不许执行。
写一个测试：让系统尝试自动执行一个红灯动作，必须被拒绝并留下记录。

按规矩五，先给我三样。
```

---

# 万能话术（随时用）

## 它跑偏了

```
停。你在做 ____，我要的是 ____。
别解释也别道歉，告诉我两件事：
① 你为什么理解成那样？（我想知道我哪句话有歧义）
② 已经改的东西里，哪些要退回去？
```

## 它说做完了

```
按规矩，汇报这四条：
① 改了哪几个文件？各改了什么？
② 怎么验证的？把你实际跑的命令和真实输出贴出来
③ 有没有想做但没做成的？是什么？为什么？
④ 有没有可能弄坏别的地方？哪儿？
```

## 它一次想做太多

```
一次只做一件。把你想做的其他事写下来，我们排队，别现在动。
```

## 开新对话时（续上下文）

```
接着上次。上次进度：____（贴它上一次的四条汇报）

别重新读整个仓库，只读你这次要改的那几个文件。
今天做：____
```

---

# 一条提醒

**每一条派活，都以「按规矩五，先给我三样」结尾。**

那三样是：**怎么做 / 我怎么验证它真的成了 / 最可能出问题的地方。**

**顺序不能反。** 先做完再想怎么验，它会给你一个刚好能通过的验法。
