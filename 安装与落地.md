# 安装与落地 · 第一版

**这份回答四个问题**：装在哪、怎么接 API、手机怎么办、整套东西长什么样。

---

# 一、先说清楚：它是什么形态

**MoneyLoop 是一个在你电脑上运行的命令行程序。**

不是网站、不是 App、不需要服务器、不需要联网到我们这边——
**它跑在你自己的机器上，你的文件也全在你自己的机器上。**

```
你的电脑
 ├── loop.sh           ← 你敲命令的地方
 ├── docs/             ← 你的项目产出（全是纯文本，你随时能看能改）
 ├── work/<角色>/      ← 每个角色自己的交付物
 └── .loop/            ← 状态、日志、账单
        ↓ 需要的时候调用
   Claude Code（装在你电脑上的一个命令）
        ↓ 联网
   Anthropic 的 API
```

**所以：需要一台电脑，这一步绕不过去。** Mac、Windows、Linux 都行。

---

# 二、装（大概十分钟）

## 第 1 步：装 Claude Code

去 [claude.com/code](https://claude.com/code) 按你的系统装。
装完在终端敲一下，能出东西就算成了：

```bash
claude --version
```

**Windows 的话**：用 PowerShell 或者装个 WSL（Windows 里的 Linux 环境）都行。
这套东西是 bash 脚本，**WSL 会更稳一点**。

## 第 2 步：让它能用你的账号（两种方式，选一个）

### 方式 A · 用订阅（推荐，简单）

```bash
claude auth
```

按提示登录你的 Claude 账号。**用你订阅里的额度，不另外计费。**
额度用完会提示「session limit」，等恢复就行——
MoneyLoop 认得这个提示，会告诉你「不是代码坏了，是额度用完了，重跑没用」。

### 方式 B · 用 API Key（按量付费，能精确算钱）

去 [console.anthropic.com](https://console.anthropic.com) 拿一个 key，然后：

```bash
# Mac / Linux：写进 ~/.zshrc 或 ~/.bashrc，以后自动生效
export ANTHROPIC_API_KEY="sk-ant-你的key"

# Windows PowerShell
$env:ANTHROPIC_API_KEY="sk-ant-你的key"
```

**两种方式的区别**：

| | 订阅 | API Key |
|---|---|---|
| 怎么算钱 | 包月，用完等恢复 | 按调用量，多少算多少 |
| 适合 | 自己用、试水 | **要精确控成本、要跑无人值守** |
| MoneyLoop 的账单 | 照样记，但那是"折算价" | **就是你真实要付的钱** |

## 第 3 步：把 MoneyLoop 拿到你电脑上

```bash
git clone https://github.com/ornb985-beep/-.git moneyloop
cd moneyloop
git checkout claude/project-template-automation-ea9utn
chmod +x loop.sh
```

不会用 git 也行——在网页上点「Download ZIP」下载解压，
然后在终端 `cd` 进那个文件夹，敲一次 `chmod +x loop.sh`。

## 第 4 步：跑起来

```bash
./loop.sh budget 50                     # 先设花钱上限，这步别省
./loop.sh start "你的一句话想法"
```

**成了。** 剩下的它会带着你走。

## 装不上的时候

```bash
./loop.sh explain      # 它会用大白话告诉你现在什么情况
```

三种常见情况，处理方式完全不同：

| 它说什么 | 什么意思 | 你该干什么 |
|---|---|---|
| 「不是代码坏了，是额度用完了」 | 外部原因 | **等，别重跑**（重跑结果一样） |
| 「轮到你了，这条 AI 干不了」 | 正常停顿 | 上面写了要你干什么 |
| 「这一步没有产出 XXX」 | 真没做成 | 看 `docs/05-我不懂的.md`，里面有选择题 |

---

# 三、API 怎么接（包括以后接便宜的）

## 默认：全用官方的

装完就是这样，不用配置。

## 进阶一：某个角色用便宜的模型

在角色定义里加一行就够了。比如让「客服」用便宜模型：

```bash
# 打开 .loop/roles/客服.md，在开头加一行
模型：sonnet
```

**判断力省不得，体力活可以省。**
CEO 裁决、风控反证用最强的；客服分拣、竞品巡查用便宜的。

**实测差价**：同一个问题，`opus` $0.68，`sonnet` $0.15。**差 4 倍多。**

## 进阶二：某个角色接别家的服务

给这个角色单独建一个环境文件，**只影响他一个人**：

```bash
# .loop/roles/客服.env
ANTHROPIC_BASE_URL=https://你要接的服务地址
ANTHROPIC_API_KEY=那家的key
```

**这就是预留给你的接口。** 以后有更便宜的服务，
改这一个文件就能把某个角色切过去，其他人不受影响。

**注意**：那家服务得兼容 Anthropic 的接口格式，否则调不通。
接之前先用一个角色试，别一次全切。

---

# 四、手机怎么办（三条路，代价差一个数量级）

## 路线 A · 电脑跑，手机看 —— 今天就能用

把整个文件夹放进网盘（iCloud／坚果云／Dropbox 都行），
或者用 git 推到你自己的仓库。然后：

- 手机上打开网盘里的 **`看板.html`** → 组织图、群聊、台账、花了多少钱，全能看
- 手机上打开 **`docs/10-群聊.md`** → 所有讨论都在里面
- **想说话**：直接在手机上编辑那个文件，在末尾打字。
  回到电脑再跑命令时，**所有角色都会看到你说的话**

**代价：接近 0。** 缺点是电脑不开机的时候，活推进不了。

## 路线 B · 手机遥控 —— 要一台常开的机器

买一台便宜的迷你主机放家里，或者租一台最便宜的云服务器，
让 MoneyLoop 常驻在上面。然后手机用 SSH 客户端连上去敲命令。

- **好处**：人在外面也能推进，电脑关了不影响
- **代价**：一台机器的钱（迷你主机几百块，云服务器几十块一个月）
  ＋ 要学一点点 SSH。**但不用写任何代码。**

## 路线 C · 做成网页／App —— 这是另一个项目

整套重写成托管服务：账号体系、云端执行、计费、网页或 App 客户端。
用户什么都不用装，打开就能用。

- **代价**：`[推断]` 数月起步，而且**每个用户的 AI 调用成本要你先垫**
  （实测单次调用 $0.15–$8.20）
- **而且**：现在做等于在一个没验证的假设上压全部成本

## 我的建议：A → 验证 → 再谈 B 或 C

**理由不是保守，是这套系统自己的规矩。**

它第1步就会逼你回答「谁掏钱、卖多少单回本」，
现在跳到 C，等于**跳过验证直接下重注**——
**那正是这台机器设计出来要拦住的那种决定。**

**更稳的顺序**：A 先用起来 → 找到真实付费用户 → 再决定要不要投 B 或 C。

---

# 五、系统全景图

```
┌──────────────────────────────────────────────────────────────┐
│  你（老板）                                                    │
│  终端敲命令 ／ 手机看看板 ／ 直接在群聊文件里打字                  │
└───────────────────────────┬──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│  loop.sh · 35 条命令（跑在你自己的电脑上）                       │
│                                                              │
│   九步流水线          组织协作           控制与观察              │
│   start / go         hire / say         budget 预算闸门        │
│   back / reset       ask / 会诊          cost   花了多少        │
│                      专家团/行业报告      看板   可视化          │
│                      蒸馏/专家群/封存     auto   无人值守        │
│                      ceo / 派单 / 排班    close  结项复盘        │
│                      派活 / 验收 / 台账                         │
└───────────────────────────┬──────────────────────────────────┘
                            │ 每次调用前过预算闸门
┌───────────────────────────▼──────────────────────────────────┐
│  Claude Code（装在你电脑上）                                    │
│  每个角色：独立会话 ＋ 独立工作目录 ＋ 可指定模型/接口             │
└───────────────────────────┬──────────────────────────────────┘
                            │ 联网
┌───────────────────────────▼──────────────────────────────────┐
│  Anthropic API  ／  或你自己配的别家服务（按角色可换）            │
└──────────────────────────────────────────────────────────────┘

数据全在你自己机器上，纯文本，随时能看能改：
  docs/          项目产出（所有角色只读）
    10-群聊.md   ← 你和所有角色的对话都在这儿
  work/<角色>/   每个角色的交付物（只有他自己能动）
  references/    跨项目的判断档案（reset 也清不掉）
  .loop/         状态 / 日志 / 账单 / 角色定义
  看板.html      生成的可视化看板
```

## 组织长这样

```
        你 ──► CEO（唯一拍板的）
              ┌────┴────┐
          咨询│         │派单
              ▼         ▼
        参谋层 10    执行总监 ──► 员工（销售/内容/竞品/客服…）
        只出意见     拆单派单        干活交付
              │         │
              └────┬────┘
                   ▼
            CEO 裁决 → 验收 → 汇报你 → 回写判断档案
```

**外挂一条外部专家链**：
`专家团（CEO定缺口）→ 行业报告（找真人）→ 蒸馏 → 专家群 → 封存`

---

# 六、第一次用，我建议这么走

```bash
./loop.sh budget 30                    # 先给个小额度试水
./loop.sh start "你真正在想的那件事"      # 别拿假题目试，浪费
./loop.sh go                           # 敲到它停下来
./loop.sh 看板                          # 看一眼现在什么样
```

**跑到第4步会停下来问你 A/B/C——那一步是这套东西最值钱的地方，认真选。**

跑完九步大概 `$20~40`（实测）。**觉得不值就 `./loop.sh close` 写个复盘停掉**，
那份复盘会把教训存进 `references/判断档案.md`，**下个项目还能用。**
