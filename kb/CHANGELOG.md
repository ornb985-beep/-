# 演化留痕

> 由 `oic.kb.evolve` 自动追加。**只增不改** —— 这份文件就是「什么时候、为什么改了主意」的全部答案。

- `2026-08-05` ❌ **K-STA-024** 被 K-STA-016 推翻：样本从 7 翻倍到 11 后，ρ 由 −0.289 翻转为 +0.058，p 由 0.629 升到 0.931，Bootstrap 区间收窄到 [−0.527,+0.607] 但仍跨零。`overfit.py` 当时就给出了判据：n=7 时纯随机的 |ρ| 中位数正好是 0.289 —— 观测值等于噪声中位数。**教训：n=7 时哪怕方向讲得通也不能记。** 我当时确实标了「不显著」，但随后又写了一段解释它为什么合理，读者记住的是解释不是标注 —— 这就是过度解读的实际形态。
