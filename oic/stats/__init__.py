"""量化统计层 —— 让"结果好"这件事变得可信。

这一层**不让预测变准**，它让不确定性变诚实。

从量化金融真正该借的不是预测模型，是**防回测过拟合的工具**：
在 n=8 上测 20 个特征，最好的那个必然好看——纯属偶然。
不校正这一点，任何"我们找到了有效信号"的宣称都是自欺。

    resample.py   精确置换检验、Bootstrap 区间   —— 小样本下的正确 p 值
    overfit.py    多重检验校正、PBO             —— 防"测得多必有一个好看"
    cv.py         留一/分组交叉验证              —— 防用同批数据选特征又报成绩
"""
