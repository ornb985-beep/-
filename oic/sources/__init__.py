"""招股书与法定披露文件管线 —— 供给侧数据的真正解法。

上一轮回测暴露的死穴：需求侧数据覆盖 9/17，**供给侧只有 2/17**。
剪刀差是本系统最大的差异化，但公开渠道拿不到市占率、同业公司数、CR5。

招股书恰好有这些，而且质量高一个数量级：

    审计过 · 有法律责任 · 「行业竞争格局」章节是强制披露项

因此登记为 **A 级来源**，且是 ``PUBLIC_DOWNLOAD``（法定公开披露，
不是爬取，不触发 provenance 的 scraping 禁令）。

    edgar.py        美国 SEC：S-1 / 10-K
    cninfo.py       中国：巨潮资讯招股说明书
    filing_parse.py 章节切分 + 字段抽取，**每个数字强制过 grounding**

⚠️ 本环境出网策略阻断 EDGAR(000) 与巨潮/交易所(403)。
   取数适配器写成可注入 fetcher，测试用离线 fixture，
   在有网络的环境替换 fetcher 即可运行。
"""
