"""OIC · 商机投资委员会引擎 v4

零第三方依赖的确定性内核。

设计铁律（不可违背）：
  1. 确定性计算与 LLM 判断物理隔离 —— 本包内所有代码永不调用 LLM。
  2. 样本不足时拒绝输出，而不是输出默认值。
  3. 每个分数都能展开成一行可复算算式。
"""

__version__ = "4.0.0"

# 计算层版本号：任何影响数值结果的改动都必须 +1，
# 否则历史预测存档将无法与新结果对比（校准会被静默污染）。
SCORING_ENGINE_VERSION = 1
